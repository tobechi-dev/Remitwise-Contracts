[package]
name = "testutils"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
soroban-sdk = { version = "21.7.0", features = ["testutils"] }
soroban-sdk = { version = "=21.7.7", features = ["testutils"] }
